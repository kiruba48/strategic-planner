---
phase: 01-foundation-static-board
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/components/Visualization/RadarChart.jsx
  - src/components/Task/TaskCard.jsx
  - src/components/Board/MobileTabBar.jsx
  - src/components/Board/LaneContainer.jsx
autonomous: false
requirements:
  - KANB-01
  - MOBL-03

must_haves:
  truths:
    - "Radar chart appears on hover (desktop) or tap (mobile) as a reveal animation"
    - "Radar chart shows all 5 dimensions with correct scores from the task"
    - "Dot indicators show column position within the lane on mobile"
    - "Swipe gestures switch between lanes on mobile (complementary to tab bar taps)"
    - "Overall visual polish matches Linear-inspired dark aesthetic"
  artifacts:
    - path: "src/components/Visualization/RadarChart.jsx"
      provides: "Recharts-based radar chart component for 5-dimension scores"
      min_lines: 25
  key_links:
    - from: "src/components/Task/TaskCard.jsx"
      to: "src/components/Visualization/RadarChart.jsx"
      via: "Renders RadarChart on hover/tap state change"
      pattern: "RadarChart|TaskRadarChart"
---

<objective>
Add RadarChart visualization with hover/tap reveal on task cards, implement mobile swipe gestures for lane switching, add dot indicators for column position, and polish the overall visual experience.

Purpose: Complete the visual experience with the signature radar chart interaction ("reveal on hover") and mobile-specific navigation enhancements that make the board feel native.
Output: Task cards with interactive radar chart reveal, mobile swipe navigation, dot indicators, and production-quality dark theme polish.
</objective>

<execution_context>
@/Users/kiruba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kiruba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-static-board/01-RESEARCH.md
@.planning/phases/01-foundation-static-board/01-CONTEXT.md
@.planning/phases/01-foundation-static-board/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RadarChart component + hover/tap reveal on TaskCard + mobile enhancements</name>
  <files>
    src/components/Visualization/RadarChart.jsx
    src/components/Task/TaskCard.jsx
    src/components/Board/MobileTabBar.jsx
    src/components/Board/LaneContainer.jsx
  </files>
  <action>
    1. Create src/components/Visualization/RadarChart.jsx:
       - Import { RadarChart, Radar, PolarGrid, PolarAngleAxis, ResponsiveContainer } from 'recharts'
       - Export TaskRadarChart component accepting { scores, accentColor, size }
       - Transform scores object into recharts data array: [{ subject: 'IC', value: scores.IC, fullMark: 3 }, ...]
       - Use short dimension labels: IC, AA, OV, RS, RV
       - PolarGrid with gridType="polygon", subtle gray stroke (#374151)
       - PolarAngleAxis labels in gray-400 text, fontSize 8-9
       - Radar fill uses lane accent color with 0.3 opacity, stroke with full accent color
       - Domain always [0, 3] since scores are 1-3
       - Default size: 120px (bigger than the 80px in research — card reveal has more room)
       - Parent div must have explicit width/height for ResponsiveContainer to work (critical pitfall)
       - No PolarRadiusAxis (too cluttered at this size)

    2. Update src/components/Task/TaskCard.jsx — add hover/tap radar chart reveal:
       - Add `isExpanded` local state (useState, default false)
       - Desktop: onMouseEnter sets isExpanded=true, onMouseLeave sets false
       - Mobile: onClick/onTouchEnd toggles isExpanded (tap to reveal, tap again to hide)
       - When isExpanded=true, render TaskRadarChart below the dimension bars
       - Animate the reveal: use CSS transition (max-height transition or opacity+height)
         - Collapsed: max-height: 0, overflow: hidden, opacity: 0
         - Expanded: max-height: 140px, opacity: 1, transition: all 200ms ease-out
       - Radar chart should feel like a "reveal" per user's specific idea — smooth, not jarring
       - Pass lane accent color to RadarChart for lane-specific coloring

    3. Update src/components/Board/LaneContainer.jsx — add dot indicators for mobile:
       - Below the horizontal scrolling column area on mobile, add dot indicators
       - One dot per column (4 dots for any lane)
       - Active dot (column in view) is filled with lane accent color
       - Inactive dots are gray-600
       - Dots are small (w-2 h-2), centered horizontally, gap-2 between them
       - Track scroll position to determine which column is in view:
         - Use a ref on the scroll container
         - On scroll event, calculate which column is most visible based on scrollLeft
       - Only show dots on mobile (`md:hidden`)

    4. Update src/components/Board/MobileTabBar.jsx — add swipe gesture support:
       - Add touch event handlers for horizontal swipe on the main board area
       - onTouchStart: record start X position
       - onTouchEnd: calculate delta X
         - If deltaX > 50px rightward: switch to previous lane (chess<-hybrid<-poker)
         - If deltaX > 50px leftward: switch to next lane (chess->hybrid->poker)
       - Wrap at edges: swiping past poker goes to chess, past chess goes to poker
       - These handlers should be on the board content area (LaneContainer parent), not the tab bar itself
       - The swipe handler could be added to TaskBoard.jsx on the mobile content wrapper
       - Use a threshold to distinguish swipe from scroll (50px horizontal + must be more horizontal than vertical)

    NOTE: The swipe gesture handlers may be better placed on the TaskBoard.jsx mobile wrapper div rather than MobileTabBar. Adjust file list accordingly — the key is that swiping the board area switches lanes. If TaskBoard.jsx needs modification, update it as well.

    Visual polish pass:
    - Ensure consistent spacing across all components
    - Verify dark theme colors are harmonious (not too many competing grays)
    - Column headers should have subtle bottom border or separator
    - Cards should have subtle hover glow matching lane accent (not just bg change)
    - Overall feel: clean, fast, professional — like Linear
  </action>
  <verify>
    1. Hover over a task card on desktop — radar chart smoothly reveals below dimension bars, shows 5 dimensions with correct scores, uses lane accent color
    2. Move mouse away — radar chart smoothly collapses
    3. On mobile viewport: tap a card — radar chart reveals. Tap again — it hides.
    4. On mobile: swipe left/right on the board area — lane switches (chess -> hybrid -> poker)
    5. On mobile: dot indicators below columns show current position, update as columns scroll
    6. All transitions are smooth (200ms ease-out), no jank
  </verify>
  <done>
    RadarChart renders with correct dimension data and lane-specific accent colors. Hover/tap reveal animation works smoothly on both desktop and mobile. Mobile has working swipe gestures for lane switching and dot indicators for column position. Visual polish is consistent with Linear-inspired dark aesthetic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 board</name>
  <files>none — verification only</files>
  <action>
    Present the completed Phase 1 board for user verification. What was built:
    - Dark theme, Linear-inspired aesthetic
    - Collapsible sidebar (desktop) / bottom tab bar (mobile) for lane navigation
    - 3 lanes with correct per-lane columns (Chess: Backlog/In Progress/Testing/Done, Hybrid: Backlog/Technical Work/Stakeholder Review/Done, Poker: Backlog/Relationship Prep/Active Negotiation/Resolved)
    - Task cards with title, emoji badge, mini dimension bars, description preview
    - Radar chart reveal on hover (desktop) / tap (mobile)
    - Horizontal column scrolling on mobile with dot indicators
    - Swipe gestures for lane switching on mobile

    How to verify:
    1. Run `npm run dev` and open in browser
    2. Desktop verification (viewport > 768px):
       a. Sidebar visible on left with Chess/Hybrid/Poker options
       b. Click each lane — correct columns appear (check all 3 lane column sets)
       c. Click sidebar collapse — reduces to icons only
       d. Hover over a task card — radar chart reveals smoothly
       e. Move mouse away — radar chart collapses
    3. Mobile verification (viewport < 768px or use DevTools device emulation):
       a. Sidebar hidden, bottom tab bar visible with 3 tabs
       b. Tap each tab — lane switches with correct columns
       c. Swipe left/right on the board — lane switches
       d. Scroll columns horizontally — dot indicators update
       e. Tap a task card — radar chart reveals. Tap again — hides.
    4. Visual quality:
       a. Dark theme throughout — no white flashes or unstyled elements
       b. Lane-specific tint colors visible (blue chess, violet hybrid, amber poker)
       c. Cards have breathing room — not packed tight
       d. Overall feels clean and professional (Linear-like)

    Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>User confirms visual quality and functional behavior match requirements</verify>
  <done>User has approved the Phase 1 static board — all layout, navigation, cards, and radar chart interactions verified</done>
</task>

</tasks>

<verification>
1. RadarChart renders all 5 dimensions correctly for each task's scores
2. Hover reveal on desktop: smooth transition in and out
3. Tap reveal on mobile: toggle behavior works
4. Mobile swipe: lane switching with >50px horizontal threshold
5. Mobile dot indicators: reflect current column scroll position
6. Dark theme consistent across all components
7. Lane accent colors correctly applied (chess=blue, hybrid=violet, poker=amber)
8. No layout shifts, overflow issues, or broken scrolling
</verification>

<success_criteria>
- Radar chart visualization works on hover/tap (Phase 1 success criterion #3)
- Mobile/desktop adaptive layout complete with all navigation modes (MOBL-03)
- Board displays all 3 lanes with correct columns when switching (KANB-01)
- Visual quality meets Linear-inspired dark aesthetic standard
- User approves the visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-static-board/01-03-SUMMARY.md`
</output>
