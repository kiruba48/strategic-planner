---
phase: 01-foundation-static-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - package.json
  - src/index.css
  - src/main.jsx
  - src/App.jsx
  - src/constants/columns.js
  - src/lib/scoring/dimensions.js
  - src/lib/scoring/classifier.js
  - src/lib/seed.js
  - src/stores/taskStore.js
  - src/stores/uiStore.js
autonomous: true
requirements:
  - KANB-01
  - KANB-02
  - KANB-03
  - KANB-04

must_haves:
  truths:
    - "Project runs locally with `npm run dev` and renders a React app"
    - "TailwindCSS v4 dark theme styles are applied (dark background visible)"
    - "Zustand stores hold mock task data organized by lane and column"
    - "Column definitions exist for all 3 lanes with correct per-lane labels"
  artifacts:
    - path: "vite.config.js"
      provides: "Vite + React + TailwindCSS v4 plugin configuration"
      contains: "tailwindcss()"
    - path: "src/index.css"
      provides: "TailwindCSS v4 theme with dark mode colors and lane tints"
      contains: "@import"
    - path: "src/constants/columns.js"
      provides: "Lane and column definitions for Chess/Hybrid/Poker"
      exports: ["LANE_COLUMNS", "LANES"]
    - path: "src/lib/scoring/classifier.js"
      provides: "Score classification logic"
      exports: ["getTotalScore", "classifyTask"]
    - path: "src/stores/taskStore.js"
      provides: "Task state with persist middleware"
      exports: ["useTaskStore"]
    - path: "src/stores/uiStore.js"
      provides: "UI state (active lane)"
      exports: ["useUIStore"]
    - path: "src/lib/seed.js"
      provides: "Mock tasks across all 3 lanes and multiple columns"
      exports: ["MOCK_TASKS"]
  key_links:
    - from: "src/stores/taskStore.js"
      to: "src/lib/seed.js"
      via: "Initial state seeded with MOCK_TASKS"
      pattern: "MOCK_TASKS"
    - from: "src/lib/seed.js"
      to: "src/lib/scoring/classifier.js"
      via: "Each mock task has valid classification derived from scores"
      pattern: "classifyTask|classification"
---

<objective>
Scaffold the Vite + React 19 + TailwindCSS v4 project with dark theme, create Zustand stores, scoring library, column constants, and seed data with mock tasks across all three lanes.

Purpose: Establish the project foundation and data layer so UI components (Plan 02) can render a board with real data from stores.
Output: Running dev server with dark-themed empty React app, fully populated stores, and all data-layer modules ready for consumption.
</objective>

<execution_context>
@/Users/kiruba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kiruba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-static-board/01-RESEARCH.md
@.planning/phases/01-foundation-static-board/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project with React 19, TailwindCSS v4, and dark theme</name>
  <files>
    vite.config.js
    package.json
    src/index.css
    src/main.jsx
    src/App.jsx
  </files>
  <action>
    1. Scaffold project: `npm create vite@latest . -- --template react` (in project root since it's already initialized)
       - If project root has existing files, scaffold in a temp dir and move files, preserving `.planning/`
    2. Install dependencies:
       ```bash
       npm install tailwindcss @tailwindcss/vite zustand recharts clsx nanoid
       npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
       ```
    3. Configure vite.config.js:
       ```js
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'
       export default defineConfig({
         plugins: [react(), tailwindcss()],
       })
       ```
    4. Configure src/index.css with TailwindCSS v4 dark theme (Linear-inspired):
       - `@import "tailwindcss";`
       - `@theme` block with custom colors:
         - Base: dark backgrounds (gray-950 range: #0a0a0f body, #111118 surface, #1a1a24 card)
         - Chess lane tint: blue accent (#3b82f6 primary, #1e3a5f subtle bg)
         - Hybrid lane tint: violet accent (#8b5cf6 primary, #2d1f5e subtle bg)
         - Poker lane tint: amber/orange accent (#f59e0b primary, #5e3a1f subtle bg)
         - Text: gray-100 for primary, gray-400 for secondary, gray-600 for muted
         - Borders: gray-800 (#1f2937)
         - Sidebar: #0f0f17
       - Use `@layer base` to set `body` to dark background and light text
    5. Clean up Vite scaffold: remove App.css, default logo/counter content. App.jsx should render a minimal placeholder div with dark bg text saying "Chess-Poker Task Manager" to verify styling works.
    6. Ensure src/main.jsx imports index.css and renders App.

    IMPORTANT: Do NOT create tailwind.config.js or postcss.config.js — TailwindCSS v4 uses the Vite plugin only.
    IMPORTANT: Do NOT use `@tailwind base/components/utilities` — use `@import "tailwindcss"` (v4 syntax).
    Dark mode only — no light mode toggle per user decision.
    Linear-inspired: clean, minimal, professional dark UI aesthetic.
  </action>
  <verify>
    Run `npm run dev` — app starts without errors. Open browser: dark background visible, "Chess-Poker Task Manager" text in light color. No unstyled flash (if first-load issue occurs, refresh once — known TailwindCSS v4 dev mode behavior).
  </verify>
  <done>
    Vite dev server runs, TailwindCSS v4 dark theme renders correctly, all dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stores, scoring library, column constants, and seed data</name>
  <files>
    src/constants/columns.js
    src/lib/scoring/dimensions.js
    src/lib/scoring/classifier.js
    src/lib/seed.js
    src/stores/taskStore.js
    src/stores/uiStore.js
  </files>
  <action>
    1. Create src/constants/columns.js with LANE_COLUMNS and LANES:
       - LANE_COLUMNS keyed by lane id with per-lane column arrays:
         - chess: backlog, in-progress, testing, done
         - hybrid: backlog, technical-work, stakeholder-review, done
         - poker: backlog, relationship-prep, active-negotiation, resolved
       - LANES array: chess (♟️, 5-7), hybrid (🎲, 8-10), poker (🃏, 11-15)
       - Per user decision emoji badges: ♟️ Chess / 🎲 Hybrid / 🃏 Poker (note: user specified 🃁 for Poker but 🃏 is more widely supported — use 🃏)

    2. Create src/lib/scoring/dimensions.js:
       - Export DIMENSIONS array with 5 entries: IC (Information Completeness), AA (Agent Adaptivity), OV (Outcome Verifiability), RS (Rule Stability), RV (Reversibility)
       - Each dimension: { id, label, shortLabel, description }
       - Short labels for radar chart: Info, Adapt, Verify, Rules, Revert

    3. Create src/lib/scoring/classifier.js:
       - Export getTotalScore(scores) — sums 5 dimension values
       - Export classifyTask(totalScore) — returns 'chess' (5-7), 'hybrid' (8-10), 'poker' (11-15)
       - Export getDefaultColumn(classification) — returns 'backlog'

    4. Create src/lib/seed.js with 9-12 MOCK_TASKS across all lanes and columns:
       - 3-4 chess tasks: spread across backlog, in-progress, testing, done
       - 3-4 hybrid tasks: spread across backlog, technical-work, stakeholder-review, done
       - 3-4 poker tasks: spread across backlog, relationship-prep, active-negotiation, resolved
       - Each task has realistic title, short description, valid scores that produce correct classification
       - Use nanoid() for IDs, include order field (1000, 2000, 3000...), createdAt/updatedAt timestamps
       - Import classifyTask and getTotalScore to derive classification from scores (don't hardcode)

    5. Create src/stores/taskStore.js:
       - Zustand store with persist middleware writing to localStorage key 'chess-poker-tasks:v1'
       - Initial state: tasks seeded from MOCK_TASKS (import from seed.js)
       - Actions: addTask, updateTask, deleteTask, moveTask (for future phases)
       - Partialize: only persist tasks array
       - Wrap localStorage in try-catch per research (incognito/quota handling)
       - Selector helpers: getTasksByLane(lane), getTasksByColumn(lane, column)

    6. Create src/stores/uiStore.js:
       - Zustand store with persist middleware writing to localStorage key 'chess-poker-ui:v1'
       - State: activeLane ('chess' default), sidebarCollapsed (false default)
       - Actions: setActiveLane, toggleSidebar

    Pattern notes:
    - Flat task array, NEVER nested by lane. Derive groupings with selectors.
    - Zustand v5 double-parentheses: create()(persist(...))
    - Use createJSONStorage(() => localStorage) for persist storage option
  </action>
  <verify>
    Import stores in App.jsx temporarily: `const tasks = useTaskStore(s => s.tasks); console.log('Tasks:', tasks.length)`. Check browser console shows 9-12 tasks. Verify localStorage has 'chess-poker-tasks:v1' key with serialized data. Remove temp imports after verification.
  </verify>
  <done>
    All data-layer modules export correctly. Store initializes with mock tasks across 3 lanes. Column constants define correct per-lane columns matching KANB-02/03/04 requirements. Scoring classifier maps scores to chess/hybrid/poker correctly.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Browser shows dark-themed page with TailwindCSS styles applied
3. Console verification: taskStore has 9-12 mock tasks, uiStore has activeLane='chess'
4. localStorage contains persisted store data
5. All imports resolve (no missing module errors)
</verification>

<success_criteria>
- Vite + React 19 + TailwindCSS v4 + Zustand project runs locally
- Dark theme with lane-specific color tints defined in CSS
- Zustand stores populated with mock data across all 3 lanes
- Column definitions match KANB-02 (Chess), KANB-03 (Hybrid), KANB-04 (Poker)
- Scoring library correctly classifies scores into chess/hybrid/poker
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-static-board/01-01-SUMMARY.md`
</output>
