---
phase: 01-foundation-static-board
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/App.jsx
  - src/components/Board/TaskBoard.jsx
  - src/components/Board/LaneContainer.jsx
  - src/components/Board/KanbanColumn.jsx
  - src/components/Board/Sidebar.jsx
  - src/components/Board/MobileTabBar.jsx
  - src/components/Task/TaskCard.jsx
autonomous: true
requirements:
  - KANB-01
  - KANB-02
  - KANB-03
  - KANB-04
  - MOBL-03

must_haves:
  truths:
    - "User sees a single lane at a time with correct column labels for that lane"
    - "Desktop has a collapsible sidebar on the left for lane selection (Chess/Hybrid/Poker)"
    - "Mobile has a bottom tab bar for lane switching instead of sidebar"
    - "Columns within a lane scroll horizontally on mobile"
    - "Task cards display title, classification emoji badge, mini dimension bars, and description preview"
    - "Chess lane shows: Backlog, In Progress, Testing, Done"
    - "Hybrid lane shows: Backlog, Technical Work, Stakeholder Review, Done"
    - "Poker lane shows: Backlog, Relationship Prep, Active Negotiation, Resolved"
  artifacts:
    - path: "src/components/Board/TaskBoard.jsx"
      provides: "Root board component with sidebar (desktop) and tab bar (mobile)"
      min_lines: 30
    - path: "src/components/Board/Sidebar.jsx"
      provides: "Collapsible sidebar for desktop lane navigation"
      min_lines: 30
    - path: "src/components/Board/MobileTabBar.jsx"
      provides: "Bottom tab bar for mobile lane switching"
      min_lines: 20
    - path: "src/components/Board/LaneContainer.jsx"
      provides: "Single lane rendering its columns"
      min_lines: 20
    - path: "src/components/Board/KanbanColumn.jsx"
      provides: "Column within a lane, renders task cards"
      min_lines: 20
    - path: "src/components/Task/TaskCard.jsx"
      provides: "Task card with title, badge, dimension bars, description"
      min_lines: 40
  key_links:
    - from: "src/App.jsx"
      to: "src/components/Board/TaskBoard.jsx"
      via: "Renders TaskBoard as main content"
      pattern: "TaskBoard"
    - from: "src/components/Board/TaskBoard.jsx"
      to: "src/stores/uiStore.js"
      via: "Reads activeLane to determine which lane to display"
      pattern: "useUIStore.*activeLane"
    - from: "src/components/Board/LaneContainer.jsx"
      to: "src/stores/taskStore.js"
      via: "Selects tasks filtered by lane"
      pattern: "useTaskStore"
    - from: "src/components/Board/KanbanColumn.jsx"
      to: "src/components/Task/TaskCard.jsx"
      via: "Maps tasks in column to TaskCard components"
      pattern: "TaskCard"
    - from: "src/components/Board/LaneContainer.jsx"
      to: "src/constants/columns.js"
      via: "Reads LANE_COLUMNS for the active lane's column definitions"
      pattern: "LANE_COLUMNS"
---

<objective>
Build the complete board UI: TaskBoard with collapsible sidebar (desktop) and bottom tab bar (mobile), LaneContainer, KanbanColumn, and TaskCard components rendering mock data from Zustand stores.

Purpose: Deliver the visual kanban board experience where users can see tasks organized by lane and column, switch between lanes, and view task details on cards.
Output: Fully rendered kanban board with responsive navigation, correct per-lane columns, and task cards showing title, emoji badge, dimension bars, and description preview.
</objective>

<execution_context>
@/Users/kiruba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kiruba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-static-board/01-RESEARCH.md
@.planning/phases/01-foundation-static-board/01-CONTEXT.md
@.planning/phases/01-foundation-static-board/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build board layout — TaskBoard, Sidebar, MobileTabBar, LaneContainer, KanbanColumn</name>
  <files>
    src/App.jsx
    src/components/Board/TaskBoard.jsx
    src/components/Board/Sidebar.jsx
    src/components/Board/MobileTabBar.jsx
    src/components/Board/LaneContainer.jsx
    src/components/Board/KanbanColumn.jsx
  </files>
  <action>
    1. Create src/components/Board/Sidebar.jsx — Desktop collapsible sidebar:
       - Left sidebar with lane selection buttons (Chess ♟️, Hybrid 🎲, Poker 🃏)
       - Expanded state: shows emoji + lane label + score range (e.g., "♟️ Chess (5-7)")
       - Collapsed state: shows emoji only (icon-only mode per user decision)
       - Toggle button at bottom to collapse/expand
       - Active lane highlighted with lane's accent tint color
       - Dark theme: sidebar bg slightly darker than main content (#0f0f17)
       - Hidden on mobile (`hidden md:flex md:flex-col`)
       - Reads/writes activeLane and sidebarCollapsed from uiStore
       - Use clsx for conditional class application

    2. Create src/components/Board/MobileTabBar.jsx — Bottom tab bar:
       - Fixed at bottom of screen on mobile only (`fixed bottom-0 left-0 right-0 md:hidden`)
       - 3 tabs: Chess | Hybrid | Poker with emoji and label
       - Active tab highlighted with lane accent color, inactive tabs gray
       - Safe area padding for iOS home indicator (`pb-safe` or bottom padding)
       - Reads/writes activeLane from uiStore

    3. Create src/components/Board/LaneContainer.jsx:
       - Receives laneId prop
       - Reads LANE_COLUMNS[laneId] to get column definitions for this lane
       - Selects tasks from taskStore filtered by lane: `useTaskStore(s => s.tasks.filter(t => t.lane === laneId))`
       - Renders lane header (lane name with emoji badge)
       - Renders columns in a horizontal row with `overflow-x-auto` on mobile for horizontal scrolling
       - Each column gets `min-w-[220px]` on mobile to prevent crushing
       - Desktop: columns fill available space equally (`flex-1`)

    4. Create src/components/Board/KanbanColumn.jsx:
       - Receives column definition (id, label) and tasks array filtered to this column
       - Sticky column header with column label and task count badge
       - Vertical scrolling card list below header (`overflow-y-auto`)
       - Dark card background (#1a1a24), subtle border (gray-800)
       - Column background slightly lighter than board (#111118)
       - Comfortable spacing between cards (gap-3 per user decision: breathing room, not packed)

    5. Create src/components/Board/TaskBoard.jsx — Root board layout:
       - Desktop layout: `flex` with Sidebar on left, board content filling remaining space
       - Mobile layout: full-width board with MobileTabBar at bottom
       - Board content area renders single LaneContainer for the activeLane
       - Full height (`h-screen`) with proper overflow handling
       - Add bottom padding on mobile to account for MobileTabBar height

    6. Update src/App.jsx to render TaskBoard as the main component.

    Layout decisions per user context:
    - Single lane visible at a time (NOT all 3 simultaneously)
    - Desktop: collapsible sidebar on left
    - Mobile: bottom tab bar replaces sidebar entirely
    - Columns scroll horizontally on mobile
    - Sticky column headers per user decision
    - Linear-inspired dark aesthetic throughout
  </action>
  <verify>
    Run `npm run dev`. Desktop viewport (>768px): sidebar visible on left with 3 lane buttons, clicking each switches the displayed lane, columns show correct labels per KANB-02/03/04. Mobile viewport (<768px): sidebar hidden, bottom tab bar visible, tapping tabs switches lanes, columns scroll horizontally.
  </verify>
  <done>
    Board renders with correct layout for desktop (sidebar + single lane) and mobile (bottom tabs + single lane). All 3 lanes show their correct per-lane column labels. Columns scroll horizontally on mobile. Mock tasks from store appear in correct columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TaskCard with title, emoji badge, mini dimension bars, and description preview</name>
  <files>
    src/components/Task/TaskCard.jsx
  </files>
  <action>
    1. Create src/components/Task/TaskCard.jsx:
       - Receives task object as prop (or taskId and selects from store with selective selector)
       - Prefer selective Zustand selector: `useTaskStore(s => s.tasks.find(t => t.id === taskId))` to prevent unnecessary re-renders

    2. Default card layout (always visible):
       - Title in white/gray-100 text, semibold, truncated to 1 line if long
       - Classification emoji badge next to title: ♟️ Chess / 🎲 Hybrid / 🃏 Poker
         - Badge has subtle lane-tinted background pill (e.g., blue-tinted for chess)
       - Mini dimension bars — 5 horizontal bars showing scores at a glance:
         - Each bar represents one dimension (IC, AA, OV, RS, RV)
         - Bar width proportional to score (1/3, 2/3, or 3/3 of available width)
         - Bar color uses lane accent color (blue for chess, violet for hybrid, amber for poker)
         - Dimension label abbreviation on the left (IC, AA, OV, RS, RV)
         - Compact: all 5 bars fit in ~40px height total
       - Description preview: first 1-2 lines of description in gray-400 text, truncated with ellipsis
         - Use `line-clamp-2` Tailwind utility for 2-line truncation

    3. Card styling (dark theme, Linear-inspired):
       - Background: #1a1a24 (card surface)
       - Border: 1px solid gray-800 (#1f2937), subtle
       - Border-radius: rounded-lg (8px)
       - Padding: p-3 (comfortable spacing per user decision)
       - Hover state: slightly lighter background or subtle border glow with lane accent color
       - No shadow or very subtle shadow — Linear aesthetic is flat, not elevated

    4. Card passes taskId to KanbanColumn for rendering in the task list.

    IMPORTANT: Radar chart is NOT shown by default on cards — it appears on hover/tap in Plan 03.
    Cards should be compact but readable — title is the primary element, dimension bars are the secondary visual, description is tertiary.
  </action>
  <verify>
    Cards render inside columns with correct data from mock tasks. Each card shows: title text, correct emoji badge for its lane classification, 5 mini dimension bars with proportional widths, and truncated description text. Hover state changes card appearance subtly.
  </verify>
  <done>
    TaskCard renders all default elements (title, emoji badge, mini dimension bars, description preview) with dark theme styling. Cards are visually clean and have breathing room. Lane-specific accent colors differentiate cards across lanes.
  </done>
</task>

</tasks>

<verification>
1. Desktop: sidebar shows 3 lane options, clicking switches the active lane
2. Desktop: sidebar collapse button reduces to icons-only mode
3. Mobile (<768px): bottom tab bar visible, sidebar hidden
4. Mobile: tabs switch active lane
5. Mobile: columns scroll horizontally within the lane
6. Chess lane columns: Backlog, In Progress, Testing, Done
7. Hybrid lane columns: Backlog, Technical Work, Stakeholder Review, Done
8. Poker lane columns: Backlog, Relationship Prep, Active Negotiation, Resolved
9. Task cards show title, emoji badge, 5 dimension bars, description preview
10. Cards have comfortable spacing (not packed tight)
</verification>

<success_criteria>
- 3-lane kanban board rendered with one lane visible at a time (KANB-01)
- Chess lane columns correct (KANB-02)
- Hybrid lane columns correct (KANB-03)
- Poker lane columns correct (KANB-04)
- Mobile/desktop responsive layout with appropriate navigation (MOBL-03)
- Task cards display all required default elements
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-static-board/01-02-SUMMARY.md`
</output>
